import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const Rend = ReactDOM.createRoot(document.getElementById('rend'));
Rend.render(
  <React.StrictMode>
    <App></App>
  </React.StrictMode>
)
